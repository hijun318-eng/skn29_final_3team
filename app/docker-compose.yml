# Answervice 인프라 — 기획서 §8.2 기준 5개 소스 DB + Trino 연합 쿼리
# 사용: docker-compose up -d
# 종료: docker-compose down
# 데이터 초기화: docker-compose down -v

services:
  # =========================================================================
  # PMS DB (PostgreSQL) — 예약, 객실, 투숙, 요금
  # =========================================================================
  pms-db:
    image: postgres:16-alpine
    container_name: answervice-pms
    environment:
      POSTGRES_DB: pms
      POSTGRES_USER: pms_ro
      POSTGRES_PASSWORD: pms_ro_pass
    ports:
      - "5433:5432"
    volumes:
      - pms-data:/var/lib/postgresql/data
      - ./init/pms-init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pms_ro"]
      interval: 5s
      retries: 10
      start_period: 10s
    restart: on-failure

  # =========================================================================
  # 연회·매출 DB (PostgreSQL, PMS와 별도 DB)
  # =========================================================================
  banquet-db:
    image: postgres:16-alpine
    container_name: answervice-banquet
    environment:
      POSTGRES_DB: banquet
      POSTGRES_USER: banquet_ro
      POSTGRES_PASSWORD: banquet_ro_pass
    ports:
      - "5434:5432"
    volumes:
      - banquet-data:/var/lib/postgresql/data
      - ./init/banquet-init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    restart: on-failure

  # =========================================================================
  # POS DB (MySQL) — 주문, 매장, 상품, 결제
  # =========================================================================
  pos-db:
    image: mysql:8
    container_name: answervice-pos
    environment:
      MYSQL_DATABASE: pos
      MYSQL_USER: pos_ro
      MYSQL_PASSWORD: pos_ro_pass
      MYSQL_ROOT_PASSWORD: root_pass
    ports:
      - "3307:3306"
    volumes:
      - pos-data:/var/lib/mysql
      - ./init/pos-init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      retries: 10
      start_period: 15s
    restart: on-failure

  # =========================================================================
  # CRM DB (SQL Server) — 회원, 등급, 포인트, 고객 매핑
  # =========================================================================
  crm-db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: answervice-crm
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: Answervice123!
    ports:
      - "1434:1433"
    volumes:
      - crm-data:/var/opt/mssql
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P Answervice123! -Q 'SELECT 1' -C || /opt/mssql-tools/bin/sqlcmd -S localhost -U sa -P Answervice123! -Q 'SELECT 1'"]
      interval: 5s
      retries: 10
      start_period: 20s
    restart: on-failure

  # =========================================================================
  # 시설 운영 DB (ClickHouse) — 시설 이용, 점검, 이벤트
  # =========================================================================
  facility-db:
    image: clickhouse/clickhouse-server:latest
    container_name: answervice-facility
    ports:
      - "8124:8123"
      - "9001:9000"
    volumes:
      - facility-data:/var/lib/clickhouse
      - ./init/facility-init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    restart: on-failure

  # =========================================================================
  # Trino (연합 쿼리 엔진) — 5개 catalog 읽기 전용 조회
  # =========================================================================
  trino:
    image: trinodb/trino:latest
    container_name: answervice-trino
    depends_on:
      pms-db:
        condition: service_healthy
      pos-db:
        condition: service_healthy
      crm-db:
        condition: service_healthy
      facility-db:
        condition: service_started
      banquet-db:
        condition: service_started
    ports:
      - "8080:8080"
    volumes:
      - ./trino/catalog:/etc/trino/catalog:ro
      - ./trino/config/jvm.config:/etc/trino/jvm.config:ro
    restart: on-failure

volumes:
  pms-data:
  banquet-data:
  pos-data:
  crm-data:
  facility-data:
