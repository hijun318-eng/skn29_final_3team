# 일일보고

> 최신 보고를 이 안내문 바로 아래에 추가합니다. 날짜별 제목은 `## YYYYMMDD` 형식을 사용합니다.

## 20260811

- 오늘 한 일: 분석 차트와 표에 수치 단위를 표시하고 큰 값을 읽기 쉽게 줄여 썼으며, 지표 카드와 보고서 미리보기가 화면 크기에 맞게 배치되도록 개선했다.
- 오늘 한 일: 프론트엔드가 기본적으로 로컬 backend를 사용하도록 연결하고, 분석 화면도 명시적인 테스트 모드가 아니면 실제 Analysis API만 호출하도록 전환했다.
- 결과: 서버 오류·잘못된 응답·네트워크 실패가 예시 데이터 성공이나 이전 결과로 바뀌지 않으며, 화면 기능 연결 검사와 production build가 통과했다. 실제 브라우저 검증은 backend 통합 뒤 진행한다.

## 20260810

- 오늘 한 일: 보고서 API 경계와 제공된 목업의 compact 화면 구성을 유지하면서, 프론트엔드를 기본적으로 로컬 PC에만 공개하고 환경 변수를 명시한 경우에만 같은 네트워크에서 접속하도록 실행 설정을 보완했다.
- 결과: 기본 주소는 `127.0.0.1`, 같은 네트워크 공개 주소는 `0.0.0.0`으로 구분되고 컨테이너 이름·포트·상태 확인·빌드 설정은 유지됐다. 실제 컨테이너 빌드와 LAN `/health`·`/agent` 접속이 성공했다.
- 공유할 내용: 기존 로컬 override 파일은 수정하지 않았고 다른 컨테이너도 재시작하지 않았다. backend CORS와 Report API 연결은 이번 프론트 공개 설정의 완료 조건에 포함하지 않았다.

## 20260805

- 오늘 한 일: 분석 Agent 질문과 세 업무 데이터의 근거 확인부터 보고서 초안 생성, 목록 선택, 블록 편집, 미리보기, 확정과 확정 후 재편집까지 시연 흐름을 연결하고 기존 보고서와 새 보고서의 내용·기간을 구분했다.
- 결과: 보고서 목록을 기본 화면으로 유지하면서 드래그앤드롭 위치 선택, 블록 순서·폭 조절, 자연어 차트 생성과 생성 위치 자동 스크롤, 자동 저장을 보완했다. 완성 보고서의 제목·본문·차트·구분선 배치를 정리하고 전체 화면을 발표 자료와 같은 검정·파랑·빨강·흰색 톤으로 통일했으며 기능 연결 검사와 production build를 통과했다.
- 공유할 내용: 오늘 완성한 분석→보고서 시연 흐름으로 시연 영상을 제작할 예정이다. 데이터 카탈로그와 DB 연결 관리 화면은 발표 보조 화면으로 추가 연결하며 PDF·PPT·외부 공유는 현재 안내 동작만 제공한다.

## 20260804

- 오늘 한 일: 발표용 분석 질문과 PMS·CRM·Banquet 결과를 12열 보고서 초안으로 옮기는 편집 흐름을 만들고, Report 정의 생성·조회·승인·다음 초안·실행 이력을 실제 서버 API로 연결했다. 정의를 빠르게 바꿀 때 늦은 이전 응답이 현재 화면을 덮지 않도록 보완했다.
- 결과: 질문 전송부터 보고서 자동 배치·편집·미리보기·확정까지 확인했다. 여러 요청이 겹쳐도 모두 끝날 때까지 작업 버튼을 잠그고 재조회 성공 시 이전 오류를 지우며, 9개 API 연결·경쟁 상황 검사와 production build를 통과했다.
- 공유할 내용: 예시 데이터는 시연 설정에서만 사용하고 수동 실행은 대기 명령으로만 표시한다. worker·예약·외부 공유·PDF/PPT 내보내기는 범위 밖이며 실제 브라우저·API 화면은 후속 검증이 필요하다.

## 20260803

- 오늘 한 일: 브라우저 확대·축소와 화면 너비에 따라 주요 카드·지표가 줄바꿈되고 가용 높이를 사용하도록 반응형 화면을 보완했으며, 전체 API 오류값과 근거·단위·sampling을 표시해 분석 결과물(Artifact)을 보고서 초안에 연결하고 5개 원천 Catalog를 최신 I3 데이터 계약에 맞춤
- 결과: 작은 화면의 고객 정보·분석 지표 겹침과 잘림을 없애고 frontend 계약을 원본 `I3-DATA-v1.1.0-DRAFT`와 일치시켜 production build와 관련 frontend contract test를 재실행해 통과했으며, 기획서·요구사항정의서·화면설계서 첫 부분을 서비스 목적→사용자 문제→대표 흐름→우선순위→현재 상태 순으로 재구성하면서 상세 기술 계약과 요구사항 ID 75개를 보존
- 공유할 내용: 실제 FastAPI·Alembic 등록과 browser 접근성 확인은 R4·R1 후속 검토가 필요하며 새 dependency와 외부 서비스는 추가하지 않음

## 20260731

- `app/enterprise-react`의 UI·Report·화면 fixture 계약을 각각 `UI-v1.0.0`·`REPORT-v1.0.0`·`UI-FIXTURE-v1.0.0`으로 동결하고 백엔드 `OPENAPI-v1.0.0`과 일치시켰으며 production build와 frontend contract test를 통과
- 오늘 한 일: 분석 대화 화면에 전체 상태, 지표, 표, 차트, 데이터 출처와 조회 조건, Artifact 보고서 연결 동작을 추가하고 추가 정보가 필요한 응답을 정책 차단과 구분
- 결과: API가 제공한 값과 요청·실행 추적·Artifact·query ID를 그대로 표시하고 원천 조회 실패의 재시도 가능 여부와 부분 완료·근거 보호를 검증

## 20260730

- 최종 기획 v1.0을 기준으로 요구사항 75개와 핵심 수용 시나리오를 `01_요구사항정의서.md` v1.1로 작성하고 첨부 템플릿의 1시트·7열·병합 그룹 형식에 맞춘 공식 XLSX v1.1과 대분류·ID·본문을 양방향 동기화해 사용 범위·시각 품질을 재검증했으며, 활성 React 목업과 대조한 13개 공식 화면 ID·IA·route·요구사항 추적·P2/고객 360 차단·UI QA를 `05_화면설계서.md` v6.0으로 최종 동기화하고 WBS 1.1~1.2·5.2에 근거를 연결
- `app/enterprise-react`에서 `/customers`·`/catalog/tools` route와 메뉴를 차단하고 typed analysis client·mock·9개 UI 상태·Report version 계약 및 request/trace 흐름을 구현했으며 `DRAFT-OPENAPI-v0.1` 상태·오류 계약과 frontend contract test를 동기화
- frontend dependency 고정과 `package-lock.json`을 적용해 `npm ci` 기반 production build를 재현하고 multi-stage Dockerfile·Nginx `/health`·root Compose용 service fragment를 추가해 image build와 container health를 검증

## 20260729

- schema version `1.0.0`·seed `20260729` 기준으로 7개 서비스의 로컬 synthetic DB 볼륨을 재구축하고 6개 DB metadata, Trino catalog 6개·analytics View 8개, source readonly DDL 거부를 포함한 계약 검증을 완료
- 실행 중인 활성 frontend를 `app/enterprise-react`로 재확인해 구형 `app/react`와 빈 Django placeholder `app/django`를 제거하고 `.claude/launch.json`의 개발·preview 경로를 활성 폴더로 통일했으며 production build를 검증

## 20260728

- 구형 `app/react`를 제거하고 독립 Vite 기반 `app/enterprise-react` 목업에 분석 Agent, Customer 360, 정기 보고서, 데이터 카탈로그·연결 관리 화면을 합성 데이터(seed `20260727`, schema `enterprise-demo-v1`)로 구성했으며 7개 URL 경로, lazy page component, 공통 shell·모바일 sidebar와 typography·정렬·페이지 전환을 정비
- 기존 정기 보고서 콘텐츠와 drag and drop 2열 블록 에디터의 추가·재정렬·폭 변경·편집·삭제·차트·PDF·확정 동작을 적용하고, 데이터 카탈로그에 분산 소스→통합 쿼리→분석·보고 흐름, 연결 상태·담당 조직·Catalog Explorer·데이터 제품을 융합해 `/connections`와 연계
- 최종 기획 v1.0을 기준으로 `05_화면설계서.md`를 DataHub Core·Trino·Guarded Text-to-SQL·12-column 자동 리포팅·운영 감사와 15개 목표 화면 중심의 v5.0으로 전면 개편하고 WBS `1.6` 근거를 연결해 검증했으며, 공식 프로젝트명을 `Answervice`로 확정하고 README·React 화면·패키지·구조도·화면설계서·WBS의 활성 브랜드 표기와 파일 경로를 동기화

## 20260724

- 실시간 모니터링 운영 맵과 반응형 화면을 확대하고 현재 운영·시뮬레이션 전환 및 예상 변화 slider를 제거했으며 진입·신규 알림 수신 시 panel 자동 열림을 막고 종 아이콘으로만 열도록 변경했으며, 로그인 호텔 이미지, 전 관리자 화면에 동일 높이·여백·타이포그래피와 `실시간 연결→언어→알림→프로필→ver 1.0` 단일 구조를 렌더링하는 공통 header, 호텔 비교·VOC 운영 추이·개선 우선순위·원인 후보·점검 항목·근거 리뷰까지 library에서 2열 canvas로 drag and drop하고 1칸·2칸 폭을 전환하는 정기 보고서 editor, 기존 ivory·brown 색상 체계로 통일하고 새 대화·대화 이력을 전체 너비 header 아래에 배치한 뒤 중앙 안내 문구를 제거한 추천 질문·하단 입력형 이슈 분석 화면, v1.6 목업의 목록/대시보드·기간·플랫폼 필터·LLM 제안·감성/KPI·리뷰 목록 구성을 현재 디자인으로 옮긴 6개 채널 외부 리뷰 데이터 화면을 구현
- 호텔이 고객 여정의 세 접점에서 직접 수집한 VOC 데이터를 운영 분석에 활용하는 화면과 메뉴 명칭을 `내부 리뷰 데이터`로 통일하고, 예약별 고유 토큰의 알림톡 미리보기·LMS 폴백과 예약 확정부터 체크아웃 24시간 후 만료까지의 발송 수명주기·보안 정책을 표시했으며, 투숙객 안내에 체크인 객실 번호와 기본 펼침 의견 입력을 표시해 체크아웃 시 접근을 차단하고 숙박 리뷰는 체크아웃 후 24시간만 유효하도록 구현해 세 모바일 VOC 화면의 사진 첨부 metadata를 지도·최근 의견·실시간 알림에 즉시 반영하는 local event stream을 연결

## 20260723

- 영상 레퍼런스의 질문→분석→요약→예측→대응 흐름을 호텔 VOC 이슈 분석에 적용하고 호텔별 부정 VOC·대기시간·만족도 비교 차트와 우선 개선·벤치마크·표본 주의 인사이트를 대시보드·보고서에 연결했으며 합성 데이터 기준과 반응형 화면을 보완
- 관리자 로그인·세션 보호·프로필 로그아웃을 연결하고 고객용 VOC 화면은 공개 접근으로 유지했으며, 사이드바의 `이슈 분석 AGENT`·홈 이동과 `SENSE PLACE · Operation Intelligence`·`Executive Briefing` 문구를 정리하고 브라우저 다크모드와 무관한 고정 색상 체계를 적용
- 실시간 모니터링의 위험·주의 시설을 공통 알림과 연동하고 항목별 확인·삭제 및 동일 상태 재등록 방지를 적용했으며, 현황·근거 분석은 모니터링에 유지하고 대응안 선택·예상 효과·관리자 결정은 정기 보고서의 검토 흐름으로 분리해 React 소스·실행 설정과 함께 공유
## 20260722

- 관리자 정보구조를 운영 대시보드·실시간 모니터링·이슈 분석·정기 보고서·리뷰 데이터로 재편하고 데이터 관리 메뉴 삭제, 리뷰 데이터 하단 배치, 공통 헤더 타이포·비율, 주차 선택 드롭다운, `호텔 운영 및 VOC 대시보드` 제목과 총괄 매니저 `박준희` 프로필을 반영
- 호텔 지도 레퍼런스를 바탕으로 운영 맵의 상단 진입도로·중앙 순환도로·강변 연결도로와 입체형 건물군을 재구성하고 마커·안내 카드 충돌과 의미가 불명확한 조식 대기열 점·중복 상태 노선을 제거
- 조식당·프런트·주차장·객실층·로비·컨벤션 선택 시 시설별 핵심 지표, 관측 사실, 원인 후보, 반대 근거, 최근 VOC, 권장 조치와 관리자 의사결정이 오른쪽 상세 패널에 표시되도록 구현
- SENSE PLACE 고객 접점인 부대 시설 만족도·체크인 후 투숙객 안내·체크아웃 후 숙박 리뷰 모바일 화면을 구현하고 세 화면을 리뷰 데이터 탭으로 통합해 QR·문자·카카오톡 링크 기반 VOC 수집 흐름을 구성
- 정기 보고서를 의사결정 중심으로 개편하고 대시보드 AI 질문이 이슈 분석 브리핑·근거·원인 후보·후속 질문·운영 맵 확인으로 이어지도록 구현했으며 전체 관리자·고객 IA, 최신 헤더·필터·맵·보고서 정렬, 상태, 데이터 계약과 UI QA를 화면설계서 v3.2에 반영

## 20260721

- 호텔 관리자 의사결정 중심으로 운영 홈의 Executive Summary→KPI→주요 이슈 TOP3→VOC·운영 비교→원인 후보·대응 옵션→보고 검토 순서와 UI QA를 구체화
- AI 결과의 과도한 확정 표현과 자동 실행 오해를 제한하고 관리자 승인·보류·반려 흐름과 안전 표현을 보완했으며 화면설계서 기준본 버전을 `2.0`으로 변경
- 요청에 따라 AI 시스템 아키텍처 초안을 삭제하고 WBS `5.6`과 제출 일정 상태를 `대기`로 복원
- 현재 WBS 63개 태스크와 공식 산출물 21건+옵션 1건을 드롭다운·상태색·진척률·평일 간트가 포함된 Excel 산출물로 동기화
- React 운영 홈에 간소화 호텔 운영 맵, TMAP식 혼잡 표시, 대응안·시간 변화 비교와 관리자 승인·보류·반려 시연 흐름을 통합

## 20260720

- 이상징후 발견부터 운영 이슈 Case 생성, 원인 후보 조사, 담당자 조치, 효과 확인과 주간 리포트로 이어지는 비즈니스 업무 흐름 구체화
- 호텔 대상 B2B SaaS 수익모델·도입 효과 지표를 정리하고, 중간발표 화면설계를 역할 선택·운영 홈·대화형 분석과 이상징후·이슈 브리프·보고 승인으로 이어지는 6개 화면·2개 독립 경로로 최신화

## 20260716

- 호텔 VOC AI Agent 요구사항 68개를 Core·Enhanced·차기로 구분하고 데이터 계약, KPI·평가·성능 기준과 수용 기준을 보완
- 공식 산출물과 평일 일정, PM 휴가·대행 체계를 반영해 WBS를 재구성하고 일별 간트와 선택형 드롭다운을 적용
- 요구사항 정의서와 서비스 흐름을 바탕으로 IA, 사용자 여정, 화면 상태, 요구사항 추적과 반응형 와이어프레임을 포함한 화면설계서를 작성하고 호텔형 UI 디자인을 보강

## 20260715

- WBS, 서비스 흐름도, 호텔 VOC 요구사항 정의서를 정리

## 20260710

- 노션 협업 도구 구축
