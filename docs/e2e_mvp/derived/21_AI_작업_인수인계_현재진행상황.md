# AI 작업 인수인계 — 현재 진행 상황

## 최신 P0 판정 (2026-08-12 23:40 KST)

결론: P0 Exit Gate를 완료 처리한다. 공식 Docker·HTTP·DB·Trino·DataHub·GPT·브라우저 경로에서 1 Source와 PMS·CRM·POS 3 Source Golden E2E, Analysis 저장·재실행·이력, Artifact, Report 수동 실행·부분 실패, 인증·권한·Trino read-only, 전체 무볼륨 재시작과 영속성을 확인했다. 테스트나 목업 응답만으로 완료 판정하지 않았다.

현재 Node 1·2·Repair·3은 모두 OpenAI provider의 `gpt-5.4-mini`를 사용한다. 기존 로컬 sLLM 설정은 `.env`에서 주석으로만 보존하고 실행에 사용하지 않는다. RAG와 ML은 사용자 결정에 따라 후순위다.

최신 실제 증거:

- 공식 Compose `full`을 volume 삭제 없이 전체 재시작·재빌드했고 핵심 서비스가 모두 healthy, `/readiness`의 app_postgres·migration·approved_templates·trino·datahub·model 모두 `ready`
- 재시작 전후 DB count 동일: Analysis definition 72, Analysis request 74, Artifact 34, Query execution 34, Report definition 12, Report run 4, Report block 31, Report block run 7
- 5 Source Trino 조회: PMS 37,800 / POS 100,000 / CRM 11,475 / Banquet 2,400 / Facility 20,000
- Chrome 3 Source Golden E2E 최신 request `4189d073-f9c6-4fad-aafd-df5a34764408`, trace `a4edfc8a-8939-40b4-a464-13680c514f5a`, query `20260812_142840_00045_nty6g`, Artifact `e7c8e50a-e2ae-5f20-ae44-b8265d61a707`
- 3 Source 결과 2행: 2026-05 `143,459,521 KRW`, 2026-06 `137,954,705 KRW`, 합계 `281,414,226 KRW`; 독립 Gold와 일치
- 위 경로의 G1·G2·G3·QUERY·ARTIFACT 통과, Node 2 prompt `node2.sql@PROMPT-v1.2.2`, Node 3 prompt `node3.explain@PROMPT-v1.2.0`
- Node 2의 실제 GPT 호출은 유지한다. 승인된 3 Source Business Rule에서는 모델 SQL을 그대로 실행하지 않고 Approved Context 기반 결정론적 compiler로 정규화한 뒤 G2·Trino·G3를 통과시킨다. KPI·행·합계는 코드에 넣지 않으며 실제 Trino 결과를 사용한다.
- 무인증·잘못된 token의 Analysis `401`, analyst token의 Report 승인 `403`
- Trino read-only write negative query `20260812_143347_00113_nty6g`: `PERMISSION_DENIED`, PMS 행 수 `37,800 → 37,800`
- Report API: definition 8개, run 4개, status `success`·`partial`; 부분 실패 run `8d7326ca-7768-421f-b3e2-fbe36a192327`에서 Block `failed,success` 분리
- Frontend 제품 source의 mock·fixture·dummy·fake·고정 검증 금액·localhost endpoint 검색 0건, contract test와 Vite production build 통과
- Backend 전체 회귀 `320 passed`, `18 skipped`, 121 subtests; Compose integration 3건 별도 통과
- 브라우저 증거: `p0_analysis_three_source_success.png`, `p0_report_partial_failure.png`

## P0 재검토와 Frontend 하드코딩 제거 (2026-08-12)

이 절은 당시 검토 기록이다. 최신 P0 판정은 위 절을 따른다. P1의 Schedule·Report Assistant·MCP 구현은 그대로 보존하며, RAG·ML은 사용자 결정에 따라 후순위로 둔다.

Frontend 제품 코드에서는 다음 고정 데이터를 제거했다.

- Analysis·Catalog·Enterprise demo fixture 파일과 fixture/mock 전환 분기
- 고정 질문, 고정 조회 기간, 가짜 최근 분석, 가짜 실행 trace
- `localStorage`·`sessionStorage` 기반 Report 샘플 저장
- Demo Organization, 고정 Source 개수, 미지원 Catalog·Connections·Customer 360 페이지와 라우팅
- 고정 컬럼명·단위 매핑과 고정 분석 제목
- 하드코딩된 인증 fallback token

Agent와 Report는 사용자 입력 또는 실제 Backend 응답만 표시한다. API 오류 시 샘플 데이터로 전환하지 않고 오류·빈 상태를 그대로 표시한다. UI 레이블, API 계약 enum, 레이아웃 값은 제품 데이터가 아니므로 유지했다.

재검토 증거:

- 신규 실제 Analysis request `e3381fe2-d860-47f9-aa61-3de88a3a42a6`, status `SUCCEEDED`
- trace `e4fba275-0926-4c51-9e2e-5ab1be622b34`
- Trino query `20260812_121056_00992_99hzd`, 30행
- Artifact `8f9cafd9-8632-5598-9dd2-87d3e57b5f6e`
- 실제 Audit에서 Node 1·2·Repair·3의 서로 다른 prompt ID와 hash 확인
- migration head `20260812_12`
- App DB: Analysis definition 56, run link 57, Report definition version 11, Report run 4, schedule 1, assistant request 5
- Trino serving view 8개, `hotel_daily_metrics` 2,308행
- 무인증 Analysis API `401`, readiness `ready`
- 실제 브라우저에서 Agent의 빈 입력·실제 Saved Analysis/Run History와 Report의 실제 정의 목록 확인
- Frontend contract test와 Vite production build 통과, Frontend Docker image 재생성 후 healthy
- 제품 코드 검색에서 fixture/mock/demo·고정 날짜·browser storage 잔여 0건
- `git diff --check` 통과

전체 Python 회귀는 이번 로컬 shell에 FastAPI·Alembic·pytest 개발 의존성이 없어 재실행하지 못했다. 직전 동일 Backend 기준 전체 결과 `306 passed`, `18 skipped`는 유지되며, 이번 변경 범위인 Frontend는 별도 계약 테스트·production build·Docker·실제 HTTP/브라우저로 검증했다.

## P1 일정 기반 Report 완료 (2026-08-12)

- owner-scoped `report_v1.report_schedules`와 migration `20260812_10`을 적용했다.
- `daily`·`weekly`·`monthly`, `Asia/Seoul`, timezone-aware 시각을 계약으로 고정했다.
- 승인된 본인 소유 Report version만 예약할 수 있다.
- due 실행은 검증된 수동 Report 실행기를 재사용해 Artifact·Query 검증과 Block별 상태 판정을 동일하게 적용한다.
- 예약 시각 기반 idempotency key로 같은 due 호출의 중복 실행을 차단한다.
- REPORT_ADMIN 생성·목록·due 실행 API와 실제 Frontend 예약 UI를 연결했다.
- `hotel_analyst`의 예약 API 접근은 실제 HTTP `403`으로 차단됐다.

실제 증거:

- schedule `bfa08438-0cfd-42c8-875e-409c66ebb25a`
- definition `2eafa6c3-b97e-49de-be37-bc7d47aa6fad` v1
- run `f41f0abe-337b-4081-9f9b-ece1bbc360e1`, status `success`
- 다음 실행 시각 `2026-08-13 20:33:43+09:00`으로 전진
- 같은 `now`의 두 번째 due 호출은 `executed=false`, `run=null`
- DB에서 schedule·last_run_id·run status·next_run_at 연결 확인
- 관련 backend `19 passed`, `14 skipped`, 14 subtests passed
- OpenAPI drift, Frontend contract, production Docker build, `git diff --check` 통과

Report Assistant까지 완료됐다. RAG·ML은 후순위이며 MCP는 아래 최소 read-only Slice로 완료했다.

## P1 Report Assistant 완료 (2026-08-12)

- 승인된 Analysis Artifact의 narrative·evidence·chart metadata만 모델 입력으로 사용하며 원본 결과를 다시 계산하지 않는다.
- Node 1·2·Repair·3과 다른 전용 `report.assistant@PROMPT-v1.0.0`을 사용한다.
- Assistant는 제목·요약·표 제목·차트 제목만 제안하며 SQL 생성, 승인, 실행을 금지한다.
- 결과는 text·table·chart 3개 Block의 owner-scoped draft로 저장되고 기존 Artifact ID와 Query ID를 그대로 보존한다.
- instruction 원문은 DB에 저장하지 않고 SHA-256 hash만 기록한다.
- 성공·실패, prompt ID/version/hash, model version, definition 연결을 `report_v1.report_assistant_requests`에 저장한다.
- 모델 실패 시 HTTP `502`, 실패 audit를 남기고 Report definition은 생성하지 않는다.
- `hotel_analyst` 접근은 실제 HTTP `403`으로 차단했다.

실제 성공 증거:

- assistant request `580a9ed9-e763-4dff-a474-91ab27d1efac`
- definition `1418c508-621e-453f-8b98-7662877e59e5` v1, status `draft`
- model `gpt-5.4-mini`, prompt `report.assistant@PROMPT-v1.0.0`
- source Artifact `8426d14d-21cc-5353-8257-272cebf0beb5`
- source Query `20260812_103809_00349_jhdbv`
- 실제 브라우저에서 생성 draft와 3개 Block, request·prompt·model·duration 표시 확인

실제 실패 증거:

- assistant request `437565a8-fc2d-4615-9a28-2324a1bf3dad`
- 의도적으로 닫은 일회성 model endpoint에서 HTTP `502`
- audit `failed|MODEL_FAILED`
- 실패 전후 Report definition count `9 -> 9`
- 일회성 실패 검증 컨테이너는 종료 후 자동 제거됨

당시 migration head는 `20260812_11`이었다. 관련 검증은 `40 passed`, `14 skipped`, 21 subtests passed이며 OpenAPI drift, Frontend contract, production Docker build, `git diff --check`도 통과했다. 현재 head와 다음 순서는 아래 MCP 항목을 따른다.

## P1 MCP 최소 Vertical Slice 완료 (2026-08-12)

- 공식 MCP `2026-07-28`의 stateless Streamable HTTP 요청 형식으로 `POST /mcp`를 제공한다.
- 승인 Tool은 `analysis.get_run@1.0.0` 하나이며 `readOnlyHint=true`, `destructiveHint=false`다.
- Tool은 기존 `PostgresAnalysisRepository.get_run()`을 재사용해 인증된 `hotel_analyst` 본인의 저장된 Run만 조회한다.
- Bearer 인증, `MCP-Protocol-Version`, `Mcp-Method`, `Mcp-Name`, client `_meta`를 fail-closed로 검증한다.
- Origin이 존재하면 승인된 Origin만 허용한다.
- Tool Registry와 성공·실패·거부 실행 근거는 `tooling` schema에 저장한다.
- RAG·ML table, 외부 Tool, write Tool, MCP 관리 UI는 만들지 않았다.

실제 성공 증거:

- Tool Run `e85a7760-27a3-4211-baf9-edcf58c25f61`
- trace `mcp-success-final`, status `SUCCEEDED`
- 조회 request `e3381fe2-d860-47f9-aa61-3de88a3a42a6`
- 반환 query `20260812_121056_00992_99hzd`
- 반환 Artifact `8f9cafd9-8632-5598-9dd2-87d3e57b5f6e`

실제 차단 증거:

- 무인증 `401`
- `report_admin`의 Tool 목록은 0개
- 직접 호출은 JSON-RPC `-32001`, Tool Run `4c86e305-4763-4835-8a53-e577b0e7f20c`, status `DENIED`, error `ACCESS_DENIED`
- 미지원 protocol version `400`, 미승인 Origin `403`, 미등록 write Tool `-32602`, clientInfo 누락 `-32602`

현재 migration head는 `20260812_12`다. 전체 Backend 회귀는 Docker CLI 의존 3건을 제외한 컨테이너에서 `316 passed`, `18 skipped`, 120 subtests passed이고, Compose 3건은 host에서 별도 통과했다. Frontend contract·production build, OpenAPI drift, `git diff --check`도 통과했다. 최신 Backend image는 `sha256:70749304efbae5b7cfb1c2eea9302236c9f94fbe82ef0bb2f65eee8795174045`이며 `healthy`다.

작성 시각: 2026-08-12 (Asia/Seoul)

## 현재 상태

- P0의 1 Source 및 PMS·CRM·POS 3 Source 공식 Docker·HTTP·DB·Trino·DataHub·브라우저 Golden E2E와 저장·Report·보안·재시작 경로를 모두 통과했다.
- strict G2는 완화하지 않았다. 3 Source는 Approved Context filter parameter mapping과 승인 Business Rule compiler를 적용해 통과했다.
- P1의 일정 기반 Report와 Report Assistant까지 구현·검증됐다.
- P1 MCP read-only 최소 Slice까지 구현·검증됐다.
- RAG와 ML-as-a-Tool은 사용자 결정에 따라 후순위다.
- `source/` 원문은 수정하지 않았다. 이 문서는 현재 런타임 사실만 기록한다.
- commit, push, PR, 외부 배포와 rollback volume 삭제는 수행하지 않았다.

## 공식 서비스와 데이터

- 공식 Compose project: `hotel-synthetic-db`
- 공식 Source DB: PMS PostgreSQL, POS MySQL, CRM SQL Server, Facility ClickHouse, Banquet PostgreSQL
- 공식 Trino: `http://127.0.0.1:18080`
- 공식 Backend/Frontend: `http://127.0.0.1:28000`, `http://127.0.0.1:13000`
- App PostgreSQL migration head: `20260812_12`
- DataHub: v1.7 Core, Source·serving ingestion 303 records
- serving analytics view: 8개, 117 columns
- AssetBinding: 8개 모두 `VERIFIED`
- 공식 데이터 snapshot: `seed_version=20260729`, `schema_version=1.0.0`
- `recognized_room_revenue`는 checkout 인식 기준, `stay_day_allocated_room_revenue`는 투숙일 배분 기준으로 분리했다.

Gold 결과와 canonical hash:

- 30행, 합계 `683,250,000 KRW`
- SHA-256 `f52f170561ff923c80080b067f296d5b78be19819c756ff0acd7d2240142b170`

교체 전 rollback volume과 검증용 volume은 삭제하지 않았다. 검증용 `service_demo_v3`, `answervice-v3-smoke` 컨테이너와 오래된 one-off backend 컨테이너만 중지했다.

## 모델과 프롬프트

Node 1·2·Repair·3은 `.env`의 OpenAI-compatible endpoint와 `gpt-5.4-mini`를 사용한다. Node 2 provider adapter 경계는 유지하며, 주석 처리한 sLLM 설정은 현재 실행에 사용하지 않는다.

각 Node는 서로 다른 prompt와 strict output schema를 사용한다.

| Node | Prompt | 역할 |
|---|---|---|
| Node 1 | `node1.normalize@PROMPT-v1.2.0` | 질문·지표·기간 구조화 |
| Node 2 | `node2.sql@PROMPT-v1.2.2` | Approved Context 안의 read-only Trino SQL 생성 |
| Node 2 Repair | `node2.repair@PROMPT-v1.2.2` | 정규화된 G2 오류만 1회 수정 |
| Node 3 | `node3.explain@PROMPT-v1.2.0` | Safe Result와 Evidence만 설명 |

실제 Audit trace에는 Node별 model, prompt ID/version/hash, duration, attempts와 status를 기록한다. Secret과 chain-of-thought는 기록하지 않는다.

확인 요청 `e4bfec11-5cdc-4141-ae77-c524b4d97e5f`에서 Node 2 초안은 필수 metric filter 누락으로 G2에 차단됐고, 서로 다른 Repair prompt가 1회 보완한 뒤 G2·Trino·G3를 통과했다.

## 실제 사용자 흐름 증거

브라우저에서 다음을 직접 조작해 확인했다.

- 질문·기간 입력 → GPT Node 1/2/Repair/3 → Trino → 표·차트·설명·Evidence
- 2026년 5월 checkout 인식 객실 매출 31행, 합계 약 `8.48억 원`
- request `7c7e8703-63fe-47b5-b8fa-1ee60f4ca357`
- Trino query `20260812_110609_00234_99hzd`
- artifact `6cdd0bba-ff09-582b-8f98-97dd938bb2e1`
- Saved Analysis 저장 Definition `e89e8783-053c-4b18-9ee6-f0bdb660d311`
- Saved Analysis 기간 변경 재실행 request `03906464-3572-4ff7-b0f0-577a8ea84ec8`, query `20260812_111328_00318_99hzd`
- Run History에서 과거 request/query/artifact 불변 보존 확인
- Report Definition·Block 저장, 승인, 수동 실행, 성공과 부분 실패 Block 표시 확인
- Report 성공 run `3d09d7f4-1f18-495e-918b-d7ffadc73e9a`
- Report 부분 실패 run `8d7326ca-7768-421f-b3e2-fbe36a192327`
- 390px viewport에서 가로 overflow 없이 Golden Path와 Saved Analysis 표시 확인

프런트 제품 하드코딩 제거 후 2026-08-12에 브라우저 Golden E2E를 다시 실행했다.

- 사용자 입력: `2026년 6월 체크아웃 기준 객실 매출을 일별로 분석해줘.`, 기간 `2026-06-01`~`2026-07-01` 미포함
- 화면 결과: status `success`, `READY`, 30행, 합계 `726,450,000 KRW`
- request `485c0688-d812-4e07-9003-86e07df38852`
- trace `c875dfcb-a6b5-4dcd-af92-307d146f678d`
- Trino query `20260812_123757_01329_99hzd`
- artifact `2c46caee-0569-5e46-a261-d782d3f1b866`
- Saved Analysis Definition `88a639a0-0dc0-40c7-948f-bdab50456ffd`, 새 브라우저 로드 후 재조회 확인
- Report draft Definition `9b2f4805-454e-4ab6-a144-b9d822b35f93`, v1 draft, 새 브라우저 로드 후 재조회 확인
- 자동화 브라우저의 date input 이벤트 호환성을 위해 제출 시 실제 FormData를 읽도록 보강했다. 고정 질문·고정 날짜 fallback은 추가하지 않았다.

model readiness timeout 보강 후 공식 Backend를 재빌드하고 브라우저 회귀를 추가 실행했다.

- 단일 `/v1/models` 5초 probe는 수정 전 20회 중 5회가 약 5.1초 timeout으로 재현됐다.
- 동일 실제 HTTP probe를 최대 3회로 제한해 재시도하며, 세 번 모두 실패하면 `not_ready`로 fail-closed한다.
- 수정 후 readiness soak 50회: `50 ready / 0 not_ready`, 최대 5.853초, 5초 이상 4회는 다음 시도에서 회복
- 모호한 자연어 지표 질문은 request `67d3d09d-36ba-4233-8f38-e957a014bb8a`에서 `CONTEXT_INCOMPLETE`로 차단돼 G1 이전 fail-closed를 확인했다.
- 승인 지표를 명시한 브라우저 회귀는 30행, 합계 `907,500,000 KRW`로 성공했다.
- request `c681c443-4178-4c59-b88e-3ff8953c0f6a`, trace `8db6b53c-249b-4bcd-a3fb-61c480868f29`
- Trino query `20260812_125228_01509_99hzd`, artifact `186b9f50-4006-52a4-b23f-f9a0fc5be8c6`

## 보안·재기동·영속성

- 무인증과 잘못된 Bearer token: `401`
- `hotel_analyst`의 Report 실행 관리 접근: `403`
- Source DB write 계정과 Trino read-only 계정 분리 및 write 차단 확인
- 최신 write negative query `20260812_143347_00113_nty6g`는 `DELETE ... WHERE false`를 `PERMISSION_DENIED`로 차단했고 PMS 행 수는 `37,800 → 37,800`으로 불변이다.
- Source checksum과 canonical 결과는 negative test 전후 불변
- 공식 Source DB·Trino·Backend·Frontend·App DB 전체 재기동 후 view 8개와 이전 Artifact·Report Run 보존 확인
- 최종 readiness soak 50회 연속: app_postgres, migration, approved_templates, trino, datahub, model 모두 `ready`
- 최신 전체 재시작 직전·직후 저장 count: Analysis definition 72건, Analysis request 74건, Artifact 34건, Query execution 34건, Report definition 12건, Report run 4건

## 검증

- 데이터 validation 10~15: 위반 0행
- Source·2 Source·3 Source·5 Source Trino 조회와 독립 Gold 비교: 통과
- DataHub ingestion·semantic verifier·AssetBinding health: 통과
- 전체 Python 회귀: `320 passed`, `18 skipped`, 121 subtests; Docker CLI가 없는 테스트 컨테이너에서 제외한 Compose 3건은 호스트에서 별도 통과
- 수정 후 HTTP/OpenAPI 대상 회귀: `16 passed`, `9 skipped`
- Frontend contract: `R5 contract checks passed`
- Frontend production Docker build: 통과
- Frontend 제품 source에서 fixture/demo data, 고정 날짜, local/session storage 검색 결과: 0건
- `git diff --check`: 통과
- 별도 lint/typecheck 명령은 저장소에 구성되어 있지 않다. Python contract/pytest와 Vite production build로 검증했다.

## 남은 위험

- 실제 고객 데이터가 아니라 승인된 합성 데이터 snapshot이다.
- 3 Source compiler는 현재 승인된 `pms_crm_pos_gold_revenue_month_v1` Business Rule에 한정된다. 새로운 임의 JOIN·Metric 지원은 별도 승인 Context와 compiler 확장이 필요하다.
- 부하·장시간 soak와 외부 운영 배포는 실행하지 않았다.
- readiness probe의 순간 5초 timeout은 bounded retry로 보강해 50회 soak에서 모두 회복했다. 더 긴 시간의 외부 모델 endpoint soak와 부하 검증은 여전히 남아 있다.
- rollback volume은 의도적으로 보존 중이다.
- dirty worktree에는 이 작업 전부터 존재한 대규모 사용자 변경과 삭제가 섞여 있으므로 일괄 정리하면 안 된다.

## 다음 작업

P0 기준선을 유지하고 실제 사용자 시나리오 회귀를 추가한다. 완료된 Report Schedule·Assistant·MCP는 회귀만 유지하며, RAG·ML은 사용자 결정대로 후순위로 둔다. 다음 기능 확장 전에는 3 Source 전용 compiler의 지원 범위를 명시하고 승인 Context 단위로 확장한다.
