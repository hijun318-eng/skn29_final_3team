# 20260804 데일리 스크럼

## 오늘 팀 진행 상황

- Qwen3-4B LoRA의 긴 응답시간 때문에 제품 전환을 보류하고, Base의 응답시간·동시 실행·재시작과 승인 JOIN·기간·guided 응답을 보완한 전체 분석 경로 성공을 확인해 I3를 완료했다.
- 학습용 분석 View와 제품 Context가 다른 문제를 확인한 뒤 DataHub v1.6.0에서 View 8개·column 116개·원천 연결 17개·column lineage 90개를 실제 조회했다. 잘못된 management health 요구는 R1 계약으로 분리해 교정했다.
- 중간발표용 화면을 backend와 무관한 단일 성공 fixture로 고정하고 PMS·CRM·Banquet 분석에서 기존 드래그앤드롭 보고서 편집까지 이어지는 시연 흐름을 완성했다.

## 팀원별 발표 메모

### 박준희 (`junhee`)

- 오늘 한 일: 최대 USD 15 안에서 Qwen3-4B Base·LoRA 비교와 held-out 150건의 안전·실행 결과를 검수했다.
- 결과 및 공유: 모델 지시와 응답 계약 보완을 dev에 통합한 뒤 같은 제품 요청에서 두 달 결과와 전체 분석 경로 성공을 확인했다. I3 완료는 유지하며, 후속 DataHub 실수집의 8개 View·116개 column·17개 원천 연결·90개 column lineage를 확인하고 잘못된 health 요구를 R1 계약으로 분리했다.

### 정승 (`seung`)

- 오늘 한 일: 분석용 View 8개의 DataHub 수집 목록과 소유자 위임 조회 권한, metadata 검증 자료를 정리했다.
- 결과 및 공유: DataHub v1.6.0에서 URN 8개·column 116개·원천 연결 17개·column lineage 90개를 실제 수집·조회했다. 일반 사용자의 조회 전용 권한과 `system` catalog 차단은 유지했다.

### 윤대성 (`daesung`)

- 오늘 한 일: 실제 Base 제품 추적에서 확인된 승인 JOIN 단축·기간 타입·집계 누락을 기준으로 node2 모델 지시를 보완했다.
- 결과 및 공유: 승인된 5개 table JOIN과 직전 두 완료 월의 집계를 PROMPT-v1.0.6에 명시하고 AI 검사 47건을 통과했다. R4 보완과 함께 dev에 통합되어 같은 합성 질문의 최종 제품 trace를 통과했다.

### 김재홍 (`jaehong`)

- 오늘 한 일: 실제 Base가 긴 출처·모델 metadata를 불안정하게 생성하는 문제를 SQL-only guided 응답으로 줄였다.
- 결과 및 공유: 출처와 추적 정보는 승인 Context에서 복원하고 잉여 parameter·두 달 기간 누락을 안전 실패로 처리해 backend 검사 25건을 통과했다. R3 보완과 함께 dev에 통합되어 최종 제품 trace를 통과했다.

### 송민지 (`minji`)

- 오늘 한 일: HTTP 환경 UUID 오류와 좁은 화면의 줄바꿈·채팅 입력 폭을 개선하고 호텔명을 `Sense Place Hotel`로 통일했다. 보고서 검색·자동 초안·드래그앤드롭 블록 수정·저장·확정 흐름을 연결하고 DB 연결 카드 배치를 정리했다.
- 결과 및 공유: production build와 frontend 계약 검사를 통과했다. 고정 synthetic fixture에서 PMS 객실 매출·점유율, CRM 직접 예약 비중, Banquet 연회 변경·연계 객실 취소를 표시하고 `보고서에 담기` 후 Artifact·요약·근거 블록으로 이어지는 시연과 브라우저 오류 0건을 확인했다. 실제 스케줄 실행과 외부 공유는 수행하지 않았다.
