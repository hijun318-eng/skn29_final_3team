# 20260804 데일리 스크럼

## 오늘 팀 진행 상황

- Qwen3-4B Base와 LoRA를 같은 Gold 120건으로 비교하고 held-out 150건의 안전 검사와 실제 Trino 결과를 확인했다.
- LoRA는 실제 결과 일치가 116/120으로 향상됐지만 p95 응답시간이 43.84초로 Base 15.42초보다 길어 제품 기본값 전환을 보류했다.
- Base vLLM serving은 warm p95 0.726초·동시 2건·재시작을 통과했고 결과 회수 뒤 작업 Pod를 삭제했다.
- FastAPI 분석 경로에 Base endpoint 호출을 연결하고 실패·fallback·circuit open이 query나 Artifact 성공으로 저장되지 않도록 검증했다.

## 팀원별 발표 메모

### 박준희 (`junhee`)

- 오늘 한 일: 최대 USD 15 안에서 Qwen3-4B Base·LoRA 비교와 held-out 150건의 안전·실행 결과를 검수했다.
- 결과 및 공유: Base serving의 warm p95 0.726초·동시 2건·재시작과 task Pod 삭제를 검수했다. FastAPI의 endpoint 연결과 실패 안전 처리를 통합했으며 실제 RunPod 제품 전체 trace가 남았다.

### 정승 (`seung`)

- 보고 없음

### 윤대성 (`daesung`)

- 오늘 한 일: Qwen3-4B Base·LoRA를 비교한 뒤 고정 Base revision의 실제 vLLM endpoint를 기동해 응답시간·메모리·동시 실행·재시작을 확인했다.
- 결과 및 공유: warm p95 0.726초, 동시 2건과 재시작 readiness를 통과했다. 최대 GPU 메모리는 39,280 MiB였고 제품 기본값은 Base를 유지한다.

### 김재홍 (`jaehong`)

- 오늘 한 일: Qwen3-4B Base의 OpenAI 호환 endpoint를 기존 모델 계약 adapter와 FastAPI 분석 경로에 연결했다.
- 결과 및 공유: 고정 생성 옵션과 schema 선검증을 적용하고 timeout·잘못된 응답·fallback·circuit open을 안전 실패로 처리했다. 실제 RunPod 제품 전체 연결은 후속 확인이 필요하다.

### 송민지 (`minji`)

- 보고 없음
