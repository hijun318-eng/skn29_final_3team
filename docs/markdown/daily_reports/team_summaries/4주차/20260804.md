# 20260804 데일리 스크럼

## 오늘 팀 진행 상황

- 새 `Qwen3-4B-Instruct-2507` Base의 균형 표본 첫 건은 JSON과 안전 검사를 통과했지만 날짜 타입 오류로 Trino에서 중단했다. 일반 타입·synthetic 범위만 보완한 같은 20건 재검증을 승인했다.
- 학습용 분석 View와 제품 Context가 다른 문제를 해결했다. View를 기본으로 유지하면서 CRM 단독 3개와 승인 PMS–CRM JOIN 5개만 실제 DataHub URN·허용 column으로 공급하고 계약 밖 항목은 차단했다.
- 중간발표용 화면을 backend와 무관한 단일 성공 fixture로 고정하고 PMS·CRM·Banquet 분석에서 기존 드래그앤드롭 보고서 편집까지 이어지는 시연 흐름을 완성했다.

## 팀원별 발표 메모

### 박준희 (`junhee`)

- 오늘 한 일: 실제 제품 Context 평가의 형식·조회 행 상한·표본 편향을 분리하고 6개 업무 영역과 두 SQL 단계를 고르게 다시 검증했다.
- 결과 및 공유: 새 표본 첫 건은 날짜 타입 오류로 중단했다. Pod 삭제와 추가 비용 USD 0.0423을 확인하고, 안전 기준을 유지한 제한 재검증을 승인했다.

### 정승 (`seung`)

- 오늘 한 일: 분석 View 8개의 DataHub 수집을 검증하고 CRM 단독·PMS–CRM 경로의 raw asset 허용 목록을 고정했다.
- 결과 및 공유: View 기본 선택을 유지하면서 CRM 3개와 승인 JOIN 5개만 실제 DataHub URN으로 교정해 data test 30건을 통과했다. 신규 View·DDL·seed·recipe는 추가하지 않았다.

### 윤대성 (`daesung`)

- 오늘 한 일: 실제 Base 제품 추적에서 확인된 승인 JOIN 단축·기간 타입·집계 누락을 기준으로 node2 모델 지시를 보완했다.
- 결과 및 공유: 승인된 5개 table JOIN과 직전 두 완료 월의 집계를 PROMPT-v1.0.6에 명시하고 AI 검사 47건을 통과했다. R4 보완과 함께 dev에 통합되어 같은 합성 질문의 최종 제품 trace를 통과했다.

### 김재홍 (`jaehong`)

- 오늘 한 일: live DataHub View 연결을 유지하면서 CRM 단독과 승인 PMS–CRM 질문에만 raw Context를 공급하도록 확장했다.
- 결과 및 공유: PMS–CRM 5개·허용 column 26개만 포함하고 승인 JOIN은 통과, 누락은 차단했다. backend 검사 55건·subtest 9건과 branch 범위 검사를 통과했다.

### 송민지 (`minji`)

- 오늘 한 일: HTTP 환경 UUID 오류와 좁은 화면의 줄바꿈·채팅 입력 폭을 개선하고 호텔명을 `Sense Place Hotel`로 통일했다. 보고서 검색·자동 초안·드래그앤드롭 블록 수정·저장·확정 흐름을 연결하고 DB 연결 카드 배치를 정리했다.
- 결과 및 공유: production build와 frontend 계약 검사를 통과했다. 고정 synthetic fixture에서 PMS 객실 매출·점유율, CRM 직접 예약 비중, Banquet 연회 변경·연계 객실 취소를 표시하고 `보고서에 담기` 후 Artifact·요약·근거 블록으로 이어지는 시연과 브라우저 오류 0건을 확인했다. 실제 스케줄 실행과 외부 공유는 수행하지 않았다.
