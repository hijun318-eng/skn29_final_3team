# 20260804 데일리 스크럼

## 오늘 팀 진행 상황

- 새 `Qwen3-4B-Instruct-2507` Base의 균형 표본 첫 건은 JSON과 안전 검사를 통과했지만 날짜 타입 오류로 Trino에서 중단했다. 일반 타입·synthetic 범위만 보완한 같은 20건 재검증을 승인했다.
- 학습용 분석 View와 제품 Context가 다른 문제를 해결했다. View를 기본으로 유지하면서 CRM 단독 3개와 승인 PMS–CRM JOIN 5개만 실제 DataHub URN·허용 column으로 공급하고 계약 밖 항목은 차단했다.
- 중간발표용 프론트엔드 목업·시연 환경과 고정 임시 데이터 시나리오를 정리하고, 불필요한 Git worktree와 오래된 로컬 branch를 정리했다.

## 팀원별 발표 메모

### 박준희 (`junhee`)

- 오늘 한 일: 실제 제품 Context 평가의 형식·조회 행 상한·표본 편향을 분리하고 6개 업무 영역과 두 SQL 단계를 고르게 다시 검증했다.
- 결과 및 공유: 새 표본 첫 건은 날짜 타입 오류로 중단했다. Pod 삭제와 추가 비용 USD 0.0423을 확인하고, 안전 기준을 유지한 제한 재검증을 승인했다.

### 정승 (`seung`)

- 오늘 한 일: PPT 작업을 진행하고, 분석 View 8개의 DataHub 수집을 검증해 CRM 단독·PMS–CRM 경로의 raw asset 허용 목록을 고정했다.
- 결과 및 공유: View 기본 선택을 유지하면서 CRM 3개와 승인 JOIN 5개만 실제 DataHub URN으로 교정해 data test 30건을 통과했다. 신규 View·DDL·seed·recipe는 추가하지 않았다.

### 윤대성 (`daesung`)

- 오늘 한 일: 실제 Base 제품 추적에서 확인된 승인 JOIN 단축·기간 타입·집계 누락을 기준으로 node2 모델 지시를 보완했다.
- 결과 및 공유: 승인된 5개 table JOIN과 직전 두 완료 월의 집계를 PROMPT-v1.0.6에 명시하고 AI 검사 47건을 통과했다. R4 보완과 함께 dev에 통합되어 같은 합성 질문의 최종 제품 trace를 통과했다.

### 김재홍 (`jaehong`)

- 오늘 한 일: 향후 7일 객실 수요예측 모델과 No-show 분류를 재검증하고 SQL·RAG 근거 분리, Registry 기반 MCP·호출 제한·전역 추적 감사를 구현했다.
- 결과 및 공유: RAG 36개 테스트·검색 성능, LogisticRegression 유지, MCP·ONNX 호출 안정성을 확인했다. RAG는 P2 미승인으로 제품 활성화하지 않고 DataHub Context의 권한·범위 차단도 유지했다.

### 송민지 (`minji`)

- 오늘 한 일: 채팅·반응형 화면을 개선하고 중간발표용 프론트엔드 목업·시연 환경과 고정 임시 데이터 시나리오를 정리했다. 불필요한 Git worktree와 오래된 로컬 branch도 정리했다.
- 결과 및 공유: 서버 연결 없이도 고정된 예시 데이터로 매출 분석과 보고서 편집을 안정적으로 시연할 수 있으며, 화면 검사와 배포용 빌드를 통과했다. 실제 자동 실행과 외부 공유는 아직 연결하지 않았다.
