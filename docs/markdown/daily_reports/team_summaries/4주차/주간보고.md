# 4주차 주간보고

## 20260803 ~ 20260804 (4주차)

작성자: 3팀

## 이번 주 진행 상황

- 실제 백엔드의 권한별 캐시 격리·모델 호출 예산·동시 실행 제한을 보완하고 검증했다.
- 운영 화면에 전체 API 오류·근거·단위·sampling을 표시하고 Artifact→Report 연결과 5개 원천 Catalog를 구현했다.
- I2 전체 왕복 승인 뒤 필수 질문 30건과 Gold 120건을 포함한 평가 150건을 최종 승인했고, 전체 연결과 권한·입력 제한·SQL 차단·캐시 격리·동시 실행 제한을 다시 검증했다.
- Qwen3-4B LoRA의 실제 결과 일치 향상을 확인했지만 응답시간 증가로 제품 기본값 전환을 보류했다.

## 이번 주에 진행한 것

### 월요일 (20260803)

- 기존 migration을 바꾸지 않고 새 migration으로 빈 데이터베이스와 기존 데이터베이스를 모두 올렸다.
- Docker image normal entrypoint와 전체 readiness를 확인하고 허용 역할 성공·관리 역할 거부·실제 Trino 근거 자료 생성을 검증했다.
- 운영 화면에서 실제 합성 데이터의 성공·재질문·권한 차단·원천 장애를 확인하고 통합 검사 22건을 통과해 I2를 승인했다.
- 최신 I3 데이터 계약을 화면이 소비하도록 후속 범위를 발행하고 단계 검사 18건과 전체 integration 검사 23건을 통과했다.
- Catalog의 계약 버전 상수를 원본 I3 계약과 일치시키고 CI에서 production build와 frontend·Python·문서·역할 범위를 통과했다.
- 다섯 역할의 Wave 3 결과를 개발 브랜치에 통합하고, 다섯 원천 조회와 평가 150건의 승인 상태·내용 보존을 확인했다.
- 전체 연결 검사 23건과 권한·입력 제한·SQL 차단·캐시 격리·동시 실행 제한 검사 29건을 통과해 I3 보안 기준선을 확인했다.
- 사용자 제공 training package의 경로 안전성과 1,350건 재빌드 SHA-256 일치를 확인하고, compiled cache를 제외한 source 10개·전용 AI test 3개를 `src/ai/training`에 통합했다.
- 기존 Train·Validation 생성은 유지하면서 원장의 Gold 120건·Acceptance 30건을 명시적으로 승인·생성하도록 보완하고, split 누수 0건과 로컬 G2·Trino 150건 전수 PASS·compiled validate를 확인했다.
- 승인된 Context 밖 필드를 거부하는 SQL 생성과 1회 보정, timeout·fallback·circuit·trace를 구현하고 AI 31건·backend 소비자 23건을 통과했다.
- SQL Plan과 조회 결과 캐시를 분리하고 캐시 적중도 모든 Gate와 역할 권한을 다시 통과하도록 했으며, 동시 실행 2건과 초과 요청 HTTP 429를 검증했다.
- 전체 오류 UI와 Artifact 보고서 초안 연결, 변경 불가능한 Report 계약과 5개 원천 Catalog를 구현하고 production build와 계약 검증을 통과했다.

### 화요일 (20260804)

- Qwen3-4B를 A40 GPU에서 LoRA로 1회 학습하고 Base와 Gold 120건으로 비교했으며, held-out 150건의 안전 검사와 실제 Trino 결과를 확인했다.
- LoRA는 실제 결과 일치 116/120을 달성했지만 p95 응답시간이 43.84초로 Base 15.42초보다 길어 제품 기본값 전환을 보류했다.
- Base vLLM serving은 warm p95 0.726초·동시 2건·재시작을 통과했고 신규 비용은 약 USD 0.0628로 추정됐으며 결과 회수 뒤 작업 Pod를 삭제했다.

## 앞으로 진행할 내용

- 승인된 범위에서 Base endpoint 계약을 실제 FastAPI 제품 경로에 안전하게 연결하고 전체 trace를 확인한다.
- Report proposal의 FastAPI·Alembic 등록과 browser 접근성을 후속 검토한다.
