# 20260811 데일리 스크럼

## 오늘 팀 진행 상황

- 백엔드가 실제 데이터베이스 최신 상태와 승인된 분석 Template 1건만 준비 완료로 인정하도록 검증을 강화했다.
- 승인된 통합 매출 지표만 Node 3 설명 단계로 전달하고, 프론트엔드가 기본 로컬 주소와 명시적 LAN backend 주소를 같은 방식으로 사용하도록 연결했다.
- 기존 환경을 유지하면서 Answervice 전용 app-postgres와 backend를 별도 host port로 기동했다. `test` branch의 선택 재기동은 DB·backend·frontend의 생성 위치·Compose·환경파일이 모두 같을 때만 허용하도록 강화했다.

## 팀원별 발표 메모

### 박준희 (`junhee`)

- 오늘 한 일: 자동 검증·Answervice 전용 실행 환경과 조건부 작업 착수를 정리하고, `test` branch 선택 재기동에 생성 위치·환경파일 일치 검사를 추가했다.
- 결과 및 공유: 자동화 통합 검사 71건과 전체 통합 검사 90건을 통과했다. 서로 다른 작업 폴더에서 만든 healthy 컨테이너가 섞인 현재 환경은 재기동 전에 차단되며, 같은 생성 위치·Compose·환경파일로 다시 준비해야 한다.

### 정승 (`seung`)

- 오늘 한 일: 분석 View 8개와 필드 설명을 관리하는 Semantic Catalog 및 DataHub 게시·검증 도구를 구현했다.
- 결과 및 공유: Dataset 8개·Column 116개를 검증하고 데이터 검사 35건과 통합 검사 42건을 통과했다. CRM 8만 건과 실제 DataHub 수집은 별도 실행 승인 전이라 실행하지 않았다.

### 윤대성 (`daesung`)

- 보고 없음

### 김재홍 (`jaehong`)

- 오늘 한 일: 실제 DataHub 원천과 Trino를 연결해 승인된 통합 매출이 권한·필수 조건 검사를 거쳐 표·차트·설명·결과물로 저장되도록 했다.
- 결과 및 공유: 5월·6월 GOLD 고객 매출 2행, 합계 475,972,400원과 기준 해시를 확인했다. Python 3.12 backend 검사 137건을 통과했고 15건은 환경 조건으로 건너뛰었다. Report worker는 후속 작업이다.

### 송민지 (`minji`)

- 오늘 한 일: 프론트엔드의 기본·LAN backend 연결을 유지하면서 차트·표 단위와 큰 수 표시, 지표 카드·보고서 미리보기의 반응형 배치를 개선했다.
- 결과 및 공유: 화면 기능 연결 검사와 기본·LAN production build가 통과했고 backend 실패를 예시 데이터 성공으로 바꾸지 않는 동작을 유지했다.
