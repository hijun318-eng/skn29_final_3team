# 20260811 데일리 스크럼

## 오늘 팀 진행 상황

- 백엔드가 실제 데이터베이스 최신 상태와 승인된 분석 Template 1건만 준비 완료로 인정하도록 검증을 강화했다.
- 승인된 통합 매출 지표만 Node 3 설명 단계로 전달하고, 프론트엔드가 기본 로컬 주소와 명시적 LAN backend 주소를 같은 방식으로 사용하도록 연결했다.
- 기존 환경을 유지하면서 Answervice 전용 app-postgres와 backend를 별도 host port로 기동했다. `test` branch는 환경파일 복사 없이 승인된 절대 경로를 참조하고, 생성 위치·Compose·container name·port가 충돌하면 재기동 전에 차단하도록 강화했다.

## 팀원별 발표 메모

### 박준희 (`junhee`)

- 오늘 한 일: 자동 검증·조건부 작업 착수를 정리하고, `test` branch가 승인된 환경파일 절대 경로를 복사 없이 사용하도록 연결했다.
- 결과 및 공유: 전체 통합 검사 93건을 통과했다. 자기 작업 결과 하나는 안전 검사를 거쳐 공용 개발 기준에 직접 반영할 수 있고, 다른 사람의 여러 결과 처리는 관리자만 수행한다.

### 정승 (`seung`)

- 오늘 한 일: 분석 View 8개와 필드 설명을 관리하는 Semantic Catalog 및 DataHub 게시·검증 도구를 구현했다.
- 결과 및 공유: Dataset 8개·Column 116개를 검증하고 데이터 검사 35건과 통합 검사 42건을 통과했다. CRM 8만 건과 실제 DataHub 수집은 별도 실행 승인 전이라 실행하지 않았다.

### 윤대성 (`daesung`)

- 보고 없음

### 김재홍 (`jaehong`)

- 오늘 한 일: 실제 DataHub·Trino 통합 매출 연결을 교정하고, 사용자가 보낸 역할 대신 서버가 검증한 Bearer 인증 주체를 Analysis·Report에 공통 적용했다.
- 결과 및 공유: 실제 조회 2건과 합계 475,972,400원을 확인했다. token 원문 대신 SHA-256 값만 허용했고 Python 3.12 backend 검사 144건을 통과했으며 16건은 환경 조건으로 건너뛰었다. 운영용 인증 파일 연결은 배포 담당 작업으로 남아 있다.

### 송민지 (`minji`)

- 오늘 한 일: 차트·표·보고서 배치를 개선하고, 분석 화면이 명시적인 테스트 모드가 아니면 실제 Analysis API만 호출하도록 전환했다.
- 결과 및 공유: 서버 오류·잘못된 응답·네트워크 실패가 예시 데이터 성공으로 바뀌지 않으며 화면 기능 연결 검사와 production build가 통과했다. 실제 브라우저 검증은 backend 통합 뒤 진행한다.
