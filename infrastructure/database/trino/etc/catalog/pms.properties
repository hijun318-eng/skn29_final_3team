# 책임: PMS PostgreSQL을 runtime discovery 가능한 read-only catalog로 연결한다.
# source 장애나 credential 누락은 query 실패로 보존한다.
connector.name=postgresql
connection-url=jdbc:postgresql://pms-postgres:5432/pms_db
connection-user=${ENV:PMS_READONLY_USER}
connection-password=${ENV:PMS_READONLY_PASSWORD}
